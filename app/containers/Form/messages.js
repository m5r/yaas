/*
 * Form Messages
 *
 * This contains all the text for the Form component.
 */
import { defineMessages } from 'react-intl';

const scope = 'app.containers.Form';

export default defineMessages({
  linkLabel: {
    id: `${scope}.linkLabel`,
    defaultMessage: 'Video link',
  },
});
