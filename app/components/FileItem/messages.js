/*
 * FileItem Messages
 *
 * This contains all the text for the FileItem component.
 */
import { defineMessages } from 'react-intl';

const scope = 'app.components.FileItem';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FileItem component !',
  },
});
